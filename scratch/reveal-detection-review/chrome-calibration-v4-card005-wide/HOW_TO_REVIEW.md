# Chrome Calibration V4 Card 005 Wide Review

Open `review.html`.

This sheet is only for:

- `card-005` = Davante Adams into Jakobi Meyers

Grid details:

- 8 columns x 11 rows, read left-to-right, top-to-bottom.
- Frame 1 is the top-left image.
- Frame 9 is the old bad detector candidate.
- The sheet shows 1 second before that candidate and 10 seconds after it.

Reply like:

```text
card-005 start 48 settle 57
```

Use:

- `start <frame>` for the first frame where Jakobi Meyers starts being revealed.
- `settle <frame>` for the first frame where Jakobi Meyers is fully readable/settled.
- `manual` if this still does not show the usable reveal.

