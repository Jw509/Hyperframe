# Chrome Calibration V2 Card 027 Settle

Open `review.html`.

This grid is 6 columns x 6 rows, read left-to-right, top-to-bottom.

- Frame 1 is the top-left image.
- Frame 5 is the calibrated start time for `card-027`.
- This sheet shows 0.5 seconds before the calibrated start and 4 seconds after it.

Only reply with the settle/readable frame:

```text
card-027 settle 24
```

Use `settle not shown` if the revealed card is still not fully readable by the end of the grid.

