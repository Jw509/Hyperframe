# Normalize line endings across Windows/macOS/Linux to avoid phantom diffs
* text=auto eol=lf

# Treat known binary assets as binary (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.mp4 binary
*.mov binary
*.m4a binary
